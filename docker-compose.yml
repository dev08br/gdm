version: '3.8'

services:
  api-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: govdoc_backend_container
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1